[buildout]
package-name = ${namespace}${ndot}${nested_namespace}.${project_name}
extensions =
    mr.developer
    buildout.dumppickedversions
    buildout.minitagificator  
[instance]
http-address = $address:$http_port

